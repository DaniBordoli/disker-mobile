// Unused placeholder: TikTok WebView flow has been replaced by the native SDK.
import React from 'react';
import { View } from 'react-native';

const TikTokConnectScreen: React.FC = () => {
  return <View />;
};

export default TikTokConnectScreen;
